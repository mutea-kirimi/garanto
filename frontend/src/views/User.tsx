import {Grid, Typography} from "@mui/material";

const User = (): JSX.Element => {

    return (
        <Grid container>
            <Typography> USER </Typography>
        </Grid>
    )
}

export default User
