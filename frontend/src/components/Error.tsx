import {Grid, Typography} from "@mui/material";

const Error = (): JSX.Element => {

    return (
        <Grid container>
            <Typography> Error contact Administrator </Typography>
        </Grid>
    )
}

export default Error
