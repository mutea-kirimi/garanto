import {Grid, Typography} from "@mui/material";

const ResourceNotFound = (): JSX.Element => {

    return (
        <Grid item>
            <Typography> Resource not found </Typography>
        </Grid>
    )
}

export default ResourceNotFound